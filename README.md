## 🔄 ETL Superstore Pipeline  
Автоматизированный конвейер обработки данных для розничных продаж: загрузка сырых данных, очистка, обогащение новыми метриками и сохранение в SQLite с возможностью планирования на регулярный запуск.  

### 🎯 Цель проекта  
Разработать полнофункциональный ETL-пайплайн (Extract → Transform → Load), который:
- Загружает сырые данные о продажах из CSV-файла;
- Очищает данные, приводит даты к единому формату, удаляет дубликаты;
- Обогащает датасет новыми признаками (год, месяц, квартал, выручка по заказу);
- Сохраняет обработанные данные в SQLite для последующего анализа
- Может автоматизироваться через планировщик Windows или Airflow (для Linux/Docker).  

### 📦 Входные данные  
Источник: data/raw/superstore.csv - датасет Superstore (2015–2018)  
Размер: ~9800 записей о заказах розничной торговли  
Формат: CSV с полями Order ID, Order Date, Sales, Region, Category, Profit и т.д.  

### 🔧 Архитектура ETL  
ETL-конвейер состоит из трёх классических этапов:  
- EXTRACT (📥): Python-скрипт читает исходный CSV-файл с помощью pandas в DataFrame. На этапе загружаются 9800 исходных записей о заказах без какой-либо обработки.  
- TRANSFORM (🔧): Происходит очистка и обогащение данных. Даты приводятся к единому формату (DD/MM/YYYY → datetime), удаляются строки с пропусками в ключевых полях, добавляются новые колонки (год, месяц, квартал, выручка по заказу). На выходе получается 9800 обработанных записей.  
- LOAD (📤): Очищенные данные сохраняются в SQLite-базу superstore.db в таблицу sales. База находится в папке data/processed/ и доступна для дальнейшего анализа через SQL-запросы.  

### 🗂 Структура проекта:  
- etl_pipeline.py - основной Python-скрипт, содержащий функции extract_raw_data(), transform_data() и load_to_db(). Запускается вручную или через батник. При запуске выполняет полный цикл ETL: читает CSV, трансформирует, сохраняет в БД.  
- check_db.py - вспомогательный скрипт для проверки содержимого базы данных. Выводит топ-5 записей из таблицы sales и агрегированную статистику по годам. Используется для валидации результатов ETL.  
- run_etl.bat - батник для запуска на Windows. Позволяет запустить пайплайн двойным кликом в Проводнике вместо командной строки. Также может быть добавлен в Планировщик задач Windows для автоматического запуска.  
- data/raw/ - папка с исходными данными. Здесь лежит superstore.csv, который скачан с Kaggle или скопирован из предыдущих проектов.  
- data/processed/ - папка с результатами ETL. После запуска пайплайна здесь появляется файл superstore.db - SQLite-база с обработанными данными в таблице sales.  
- airflow_home/dags/superstore_etl_dag.py - DAG для Apache Airflow (для Linux/Docker окружений). Содержит определение задачи, которая вызывает функцию run_etl() и может быть запланирована на регулярное выполнение. На Windows Airflow не работает нативно, поэтому для локального запуска используется батник.  

### 🚀 Как запустить
#### Требования:  
- Python 3.8+ с установленной библиотекой pandas.
- SQLite встроена в Python, дополнительно устанавливать не нужно.  

##### Вариант 1: Ручной запуск из командной строки  
Открой командную строку, перейди в папку проекта и выполни:  

    cd C:\portfolio\etl-superstore-pipeline  
    python etl_pipeline.py  

Пайплайн выведет логи процесса и завершится со статусом успеха. В папке data/processed/ появится файл superstore.db.  

Ожидаемый вывод:  

    INFO:__main__:Читаю data\raw\superstore.csv  
    INFO:__main__:Загружено 9800 строк  
    INFO:__main__:Начинаю трансформацию...  
    INFO:__main__:Трансформация завершена, строк: 9800  
    INFO:__main__:Данные сохранены в data\processed\superstore.db  
    ETL пайплайн выполнен успешно  

##### Вариант 2: Быстрый запуск через батник  
Дважды кликни по файлу run_etl.bat в Проводнике. Откроется чёрное окно с тем же логом, как в варианте 1. Нажми любую клавишу, чтобы закрыть.  

##### Вариант 3: Проверка данных в БД  
После успешного запуска ETL проверь содержимое базы:  

    python check_db.py  

Скрипт выведет топ-5 записей из таблицы sales и статистику по годам:  

    TOP 5 rows:  
    ('CA-2017-152156', 261.96, 2017, 11, 993.9)  
    ('CA-2017-152156', 731.94, 2017, 11, 993.9)  
    ('CA-2017-138688', 14.62, 2017, 6, 14.62)  
    ('US-2016-108966', 957.5775, 2016, 10, 979.9455)  
    ('US-2016-108966', 22.368, 2016, 10, 979.9455)  

    Sales by Year:  
    (2015, 479856.21)  
    (2016, 459436.01)  
    (2017, 600192.55)  
    (2018, 722052.02)  

### ⏰ Автоматизация запусков  
##### Планировщик Windows (Task Scheduler)  
Чтобы ETL запускался автоматически каждый день в 02:00 без участия пользователя:  
1. Открой Планировщик задач: нажми Win+R, введи taskschd.msc, нажми Enter.  
2. В левой панели выбери "Создать базовую задачу".  
3. Укажи имя задачи (например, "Superstore ETL Daily") и описание.  
4. На вкладке "Триггер" выбери "Ежедневно" и установи время 02:00.  
5. На вкладке "Действие" выбери "Запустить программу" и укажи полный путь до батника: C:\portfolio\etl-superstore-pipeline\run_etl.bat  
6. Сохрани задачу. После этого она будет автоматически запускаться по расписанию.  

##### Airflow (для Linux/Docker)  
Если используешь окружение с Apache Airflow (например, в Docker контейнере или WSL):  
1. Положи папку проекта в dags-папку Airflow, обычно это /opt/airflow/dags/.  
2. Запусти (или убедись, что запущены) scheduler и webserver:  

    airflow scheduler &  
    airflow webserver &  

3. Открой http://localhost:8080 и найди в списке DAG superstore_etl_dag.  
4. Включи DAG переключателем и дождись запланированного времени (по умолчанию 02:00 UTC ежедневно) или запусти вручную.  

DAG автоматически вызовет функцию run_etl() из etl_pipeline.py, выполнит полный ETL и залогирует результат. При ошибках система может отправить алерт на email (настраивается в default_args DAG-файла).

### 📊 Примеры SQL-запросов  
После запуска ETL ты можешь анализировать данные прямо в SQLite. Вот несколько полезных запросов:  

###### Топ регионов по выручке:  

    SELECT Region, SUM(Sales) as total_sales  
    FROM sales  
    GROUP BY Region  
    ORDER BY total_sales DESC;  

###### Выручка по кварталам и годам:  

    SELECT Year, Quarter, SUM(Sales) as quarterly_sales  
    FROM sales  
    GROUP BY Year, Quarter  
    ORDER BY Year, Quarter;  

###### Средний размер заказа:  

    SELECT AVG(Revenue_per_order) as avg_order_value  
    FROM sales;  

###### Количество заказов по месяцам:  

    SELECT Year, Month, COUNT(*) as order_count  
    FROM sales  
    GROUP BY Year, Month  
    ORDER BY Year, Month;  

### 🛠 Технологический стек  
- Python 3.10 - основной язык разработки пайплайна. Используется для написания всей логики Extract, Transform, Load.  
- pandas 2.3.3 - библиотека для работы с табличными данными. На этапе Extract читает CSV-файл в DataFrame, на этапе Transform производит очистку, трансформацию и обогащение данных.  
- SQLite3 - встроенная СУБД Python для сохранения обработанных данных. На этапе Load используется метод df.to_sql() для записи DataFrame в таблицу БД.  
- Python logging - встроенный модуль для логирования. Выводит информацию о ходе выполнения каждого этапа ETL в консоль.  
- Windows Task Scheduler - встроенный планировщик задач для автоматизации запусков на Windows. Позволяет запускать батник по расписанию без участия пользователя.  
- Apache Airflow - опциональный оркестратор рабочих процессов для Linux/Docker окружений. Позволяет управлять сложными DAG-ами, логировать выполнение, отправлять алерты и интегрироваться с другими системами.  

### 📈 Метрики и KPI  
После обработки данных через ETL-пайплайн становятся доступны следующие метрики:  

###### GMV (Gross Merchandise Value)  
общая выручка за период. Вычисляется как сумма всех значений колонки Sales.  
###### Orders Count  
общее количество заказов. Вычисляется как количество записей в таблице sales.  
###### AOV (Average Order Value)  
средний размер заказа. Вычисляется как среднее значение колонки Revenue_per_order.  
###### Сезонность и тренды  
анализ выручки по месяцам, кварталам и годам помогает выявить сезонные паттерны и долгосрочные тренды.  
###### Региональные различия  
сравнение выручки и объёмов заказов по регионам помогает выявить географические закономерности.  

### 🚧 Возможные расширения  
- Добавить логирование в файл (logs/etl.log) вместо вывода только в консоль.  
- Расширить EXTRACT для работы с данными из API вместо статического CSV-файла.  
- Интегрировать отправку email-отчётов после каждого запуска пайплайна.  
- Подключить результаты к визуализационным инструментам (Power BI, Apache Superset, Grafana) для построения интерактивных дашбордов.  
- Добавить валидацию данных - проверки на аномалии, выбросы и логическую консистентность.  
- Реализовать Incremental Load - обновление только новых или изменённых данных вместо полной перезагрузки.  

## 📧 Контакты  
Разработчик: Ирина Васильевна  
📧 Email: irochkavinokurova@mail.ru  
📱 WhatsApp/Telegram: +7 (919) 128-29-95  

## 📄 Лицензия  
Проект использует открытый датасет Sample Superstore от Kaggle.com. Код распространяется свободно для образовательных целей.
